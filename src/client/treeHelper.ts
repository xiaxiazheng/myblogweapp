// import { baseUrl, axiosGetHelper, axiosPostHelper } from './clientHelper';

// export namespace TreeHelper {
//   export async function getTree(): Promise<any[]> {
//     const data = await axiosGetHelper(`${baseUrl}/tree?type=home`);
//     return data && data.resultsCode === 'success' ? data.data : [];
//   }
// }